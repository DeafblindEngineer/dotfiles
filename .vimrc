call plug#begin()
Plug 'ctrlpvim/ctrlp.vim'
call plug#end()

" Sets the correct colours in gnome-terminal (dark mode) to allow sighted peers to read comments easier in Vim when in a Tmux pane or window
set background=dark

